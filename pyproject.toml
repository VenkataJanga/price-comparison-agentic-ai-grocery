[tool.poetry]
name = "grocery-price-compare-agent"
version = "0.1.0"
description = "Agentic grocery price comparison"
authors = ["you <you@example.com>"]

[tool.poetry.dependencies]
python = "^3.10"
fastapi = "^0.95"
uvicorn = "^0.22"
pydantic = "^2.0"
streamlit = "^1.25"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
